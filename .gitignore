node_modules/
dist/
.env
.env.local
*.log

# Regenerable intermediates — cleared per run.
frames/
out/
test/.tmp/

# NOT ignored, deliberately (ADR-0002): `reels/` and `audio/`.
# A shipped reel and the Suno signature track cannot be regenerated — the site
# drifts, the brand kit is sampled at render, and a Suno track is not
# reproducible from its prompt. This repo is their backup. Do not "tidy" them
# into the list above.
